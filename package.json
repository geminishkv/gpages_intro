{
  "name": "intro",
  "version": "1.0.1",
  "homepage": "https://geminishkv.tech",
  "dependencies": {
    "animejs": "^3.2.2",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "sitemap": "node scripts/generate-sitemap.js",
    "blog": "node scripts/generate-blog-pages.js",
    "predeploy": "npm run sitemap && npm run build && npm run blog",
    "deploy": "node scripts/deploy.js",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "gh-pages": "^6.3.0"
  }
}
